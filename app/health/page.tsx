export const fetchCache = "force-no-store";

export default function HealthPage(): JSX.Element {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: 32 }}>
      <h1>Frontend OK</h1>
      <p>The Next.js app is rendering.</p>
    </main>
  );
}

